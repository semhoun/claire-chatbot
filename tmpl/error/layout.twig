<!doctype html>
<html lang="en">
<head>
    <!-- Meta -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Error</title>

    <!-- Minimal scoped CSS (no Bootstrap) -->
    <style>
        *, *::before, *::after { box-sizing: border-box; }
        html, body { height: 100%; }
        body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; color: #fff; }

        /* Background layers are added in <body>, these ensure stacking above them */
        .error-container { max-width: 960px; margin: 0 auto; padding: 24px; position: relative; z-index: 1; }
        .center-screen { min-height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 40px 0; }

        .error-title { font-size: clamp(48px, 12vw, 120px); line-height: 1; font-weight: 800; margin: 0 0 12px; color: #fff; text-shadow: 0 2px 16px rgba(0,0,0,0.4); }
        .error-subtitle { font-size: clamp(16px, 3.5vw, 22px); margin: 0 0 24px; color: rgba(255,255,255,0.9); }

        .btn { display: inline-block; padding: 12px 20px; border-radius: 8px; text-decoration: none; font-weight: 600; line-height: 1.2; transition: transform .06s ease, box-shadow .2s ease, background-color .2s ease, color .2s ease; }
        .btn:focus-visible { outline: 3px solid rgba(13,110,253,0.5); outline-offset: 2px; }
        .btn-primary { background: #0d6efd; color: #fff; box-shadow: 0 6px 18px rgba(13,110,253,0.35); }
        .btn-primary:hover { background: #0b5ed7; transform: translateY(-1px); box-shadow: 0 10px 24px rgba(13,110,253,0.45); }
        .btn-lg { font-size: 1.125rem; padding: 14px 24px; }
    </style>
</head>

<body>
    <div style="position:fixed;top:0;left:0;width:100vw;height:100vh;background:url('/image/background.png') center/cover no-repeat;z-index:-2;"></div>
    <div style="position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(0,0,0,0.45);z-index:-1;"></div>

<div class="error-container">
    {% block contents %}
    {% endblock %}
</div>

{% block scripts %}{% endblock %}
</body>
</html>
