{% macro draw(path, class = '') %}
<svg{% if class %} class="{{ class }}"{% endif %} viewBox="0 0 24 24" aria-hidden="true">
    <path d="{{ path }}" fill="none" stroke="currentColor" stroke-width="1.8"
          stroke-linecap="round" stroke-linejoin="round"/>
</svg>
{% endmacro %}
