{# Renders a list of messages into the chat #}
{% for m in messages %}
    {% include 'partials/message.twig' with {
        message: m.message,
        time: m.time,
        sent: m.sent,
        toolCallId: m.toolCallId,
        toolCall: m.toolText,
    } %}
{% endfor %}